<svg 
  width="28" 
  height="28" 
  viewBox="0 0 24 24" 
  fill="none" 
  xmlns="http://www.w3.org/2000/svg"
>
  <!-- Glow -->
  <defs>
    <filter id="glow">
      <feGaussianBlur stdDeviation="2.5" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- Arrow -->
  <path
    d="M12 3V14M12 14L7 9M12 14L17 9"
    stroke="#00E5FF"
    stroke-width="2.5"
    stroke-linecap="round"
    stroke-linejoin="round"
    filter="url(#glow)"
  />

  <!-- Line -->
  <path
    d="M5 19H19"
    stroke="#00E5FF"
    stroke-width="2.5"
    stroke-linecap="round"
    filter="url(#glow)"
  />
</svg>
